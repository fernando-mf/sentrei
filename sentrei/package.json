{
  "name": "sentrei",
  "license": "MIT",
  "homepage": "https://github.com/sentrei/sentrei/sentrei",
  "repository": {
    "type": "git",
    "url": "https://github.com/sentrei/sentrei/sentrei.git"
  },
  "scripts": {
    "alpha": "yarn run swap-alpha && firebase use alpha && firebase deploy --only hosting:docs --token $FIREBASE_TOKEN",
    "beta": "yarn run swap-beta && firebase use beta && firebase deploy --only hosting:docs --token $FIREBASE_TOKEN",
    "master": "yarn run swap-master && firebase use master && firebase deploy --only hosting:docs --token $FIREBASE_TOKEN",
    "staging-alpha": "yarn run swap-alpha && firebase use alpha && firebase deploy --only hosting:staging-docs --token $FIREBASE_TOKEN",
    "staging-beta": "yarn run swap-beta && firebase use beta && firebase deploy --only hosting:staging-docs --token $FIREBASE_TOKEN",
    "staging-master": "yarn run swap-master && firebase use master && firebase deploy --only hosting:staging-docs --token $FIREBASE_TOKEN",
    "swap-alpha": "cp alpha.firebase.json firebase.json",
    "swap-beta": "cp beta.firebase.json firebase.json",
    "swap-master": "cp master.firebase.json firebase.json"
  },
  "dependencies": {
    "firebase-tools": "^8.0.1"
  }
}

{
  "name": "@sentrei/design",
  "version": "3.10.2",
  "homepage": "https://sentrei.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/sentrei/sentrei.git",
    "directory": "sentrei/design"
  },
  "license": "MIT",
  "author": "Shun Kakinoki",
  "scripts": {
    "design": "yarn run swap-master && firebase use master && firebase deploy --only hosting:design --token $FIREBASE_TOKEN",
    "staging-design": "yarn run swap-master && firebase use master && firebase deploy --only hosting:staging-design --token $FIREBASE_TOKEN",
    "build": "vuepress build src",
    "dev": "vuepress dev src"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "vuepress": "^1.4.0"
  }
}

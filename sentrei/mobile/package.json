{
  "name": "@sentrei/mobile",
  "version": "3.14.0",
  "homepage": "https://sentrei.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/sentrei/sentrei.git",
    "directory": "sentrei/mobile"
  },
  "license": "MIT",
  "author": "Shun Kakinoki",
  "scripts": {
    "alpha": "yarn run swap-alpha && firebase use alpha && firebase deploy --only hosting:mobile --token $FIREBASE_TOKEN",
    "beta": "yarn run swap-beta && firebase use beta && firebase deploy --only hosting:mobile --token $FIREBASE_TOKEN",
    "master": "yarn run swap-master && firebase use master && firebase deploy --only hosting:mobile --token $FIREBASE_TOKEN",
    "staging-alpha": "yarn run swap-alpha && firebase use alpha && firebase deploy --only hosting:staging-mobile --token $FIREBASE_TOKEN",
    "staging-beta": "yarn run swap-beta && firebase use beta && firebase deploy --only hosting:staging-mobile --token $FIREBASE_TOKEN",
    "staging-master": "yarn run swap-master && firebase use master && firebase deploy --only hosting:staging-mobile --token $FIREBASE_TOKEN",
    "swap-alpha": "cp alpha.firebase.json firebase.json",
    "swap-beta": "cp beta.firebase.json firebase.json",
    "swap-master": "cp master.firebase.json firebase.json"
  },
  "dependencies": {
    "firebase-tools": "^8.0.2"
  },
  "publishConfig": {
    "access": "public"
  }
}

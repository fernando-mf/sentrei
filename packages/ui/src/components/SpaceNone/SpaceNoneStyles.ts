import {createStyles, makeStyles, Theme} from "@material-ui/core/styles";

const SpaceNoneStyles = makeStyles((theme: Theme) =>
  createStyles({
    grid: {
      minHeight: "100vh",
    },
  }),
);

export default SpaceNoneStyles;

import {createStyles, makeStyles, Theme} from "@material-ui/core/styles";

const SpaceDashbaordStyles = makeStyles((theme: Theme) =>
  createStyles({
    grid: {
      minHeight: "60vh",
    },
  }),
);

export default SpaceDashbaordStyles;

import {createStyles, Theme, makeStyles} from "@material-ui/core/styles";

const SpaceListStyles = makeStyles((theme: Theme) =>
  createStyles({
    container: {
      minHeight: "100vh",
    },
  }),
);

export default SpaceListStyles;

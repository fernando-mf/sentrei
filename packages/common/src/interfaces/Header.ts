export default interface Props {
  faqText: string;
  featuresText: string;
  logo: JSX.Element;
  pricingText: string;
  productText: string;
  loginText: string;
  signupText: string;
  sign?: boolean;
  spy?: boolean;
  testimonialText: string;
}

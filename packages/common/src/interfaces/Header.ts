export default interface Props {
  faqText: string;
  featuresText: string;
  logo: JSX.Element;
  pricingText: string;
  productText: string;
  signInText: string;
  signUpText: string;
  sign?: boolean;
  spy?: boolean;
  testimonialText: string;
}

name: Docs
on:
  push:
    branches:
      - alpha
      - beta
      - master
  repository_dispatch:
    types: [docs-cmd]
env:
  flutter_channel: "stable"
  flutter_version: "1.12.13+hotfix.9"
jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Extract branch name
        if: github.event_name == 'push'
        id: branch
        run: |
          echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
      - name: Checkout
        if: github.event_name == 'push'
        uses: actions/checkout@v2
        with:
          ref: ${{ steps.branch.outputs.branch }}
      - name: Checkout
        if: github.event.client_payload.pull_request != null
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.client_payload.pull_request.head.ref }}
      - uses: actions/setup-node@v1
        with:
          node-version: "12.x"
      - uses: subosito/flutter-action@v1
        with:
          channel: ${{ env.flutter_channel }}
          flutter-version: ${{ env.flutter_version }}
      - name: Install dependencies with yarn
        if: steps.cache.outputs.cache-hit != 'true'
        working-directory: sentrei/mobile
        run: |
          yarn install --frozen-lockfile
      - name: Generate docs
        working-directory: sentrei/mobile
        run: |
          flutter pub global activate dartdoc
          flutter pub global run dartdoc:dartdoc --exclude 'dart:async,dart:collection,dart:convert,dart:core,dart:developer,dart:io,dart:isolate,dart:math,dart:typed_data,dart:ui,dart:ffi,dart:html,dart:js,dart:js_util'
      - name: Create deployment production
        uses: chrnorm/deployment-action@releases/v1
        if: github.event_name == 'push'
        id: production
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target_url: http://${{ steps.branch.outputs.branch }}.mobile.sentrei.com
          environment: production
      - name: Create deployment staging
        uses: chrnorm/deployment-action@releases/v1
        if: github.event.client_payload.pull_request != null
        id: staging
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target_url: http://staging.${{ steps.branch.outputs.branch }}.mobile.sentrei.com
          environment: staging
      - name: Deploy to firebase
        if: github.event_name == 'push'
        working-directory: sentrei/mobile
        run: |
          yarn run ${{ steps.branch.outputs.branch }}
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      - name: Deploy to firebase
        if: github.event.client_payload.pull_request != null
        working-directory: sentrei/mobile
        run: |
          yarn run staging-${{ github.event.client_payload.pull_request.base.ref }}
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      - name: Update deployment success
        if: success() && github.event_name == 'push'
        uses: chrnorm/deployment-status@releases/v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target_url: http://${{ steps.branch.outputs.branch }}.mobile.sentrei.com
          state: success
          deployment_id: ${{ steps.production.outputs.deployment_id }}
      - name: Update deployment success
        if: success() && github.event.client_payload.pull_request != null
        uses: chrnorm/deployment-status@releases/v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target_url: http://staging.${{ steps.branch.outputs.branch }}.mobile.sentrei.com
          state: success
          deployment_id: ${{ steps.staging.outputs.deployment_id }}
      - name: Update deployment failure
        if: failure() && github.event_name == 'push'
        uses: chrnorm/deployment-status@releases/v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target_url: http://${{ steps.branch.outputs.branch }}.mobile.sentrei.com
          state: failure
          deployment_id: ${{ steps.production.outputs.deployment_id }}
      - name: Update deployment failure
        if: failure() && github.event.client_payload.pull_request != null
        uses: chrnorm/deployment-status@releases/v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target_url: http://staging.${{ steps.branch.outputs.branch }}.mobile.sentrei.com
          state: failure
          deployment_id: ${{ steps.staging.outputs.deployment_id }}
      - name: Comment url
        if: github.event.client_payload.pull_request != null
        uses: peter-evans/create-or-update-comment@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.event.client_payload.github.payload.repository.full_name }}
          comment-id: ${{ github.event.client_payload.github.payload.comment.id }}
          reaction-type: hooray
          edit-mode: replace
          body: |
            Doc updated on https://staging.${{ github.event.client_payload.pull_request.base.ref }}.docs.sentrei.com

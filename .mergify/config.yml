pull_request_rules:
  - name: sentrei
    conditions:
      - status-success=enforcer
    actions:
      delete_head_branch:
        force: true
      merge:
        method: merge
        strict: smart
